<UserControl x:Class="VDC_WPF_T.Windows.AuthWindow.RadioButtonsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:utils="clr-namespace:VDC_WPF_T.Utilities"
             xmlns:local="clr-namespace:VDC_WPF_T.Windows.AuthWindow"
             d:DesignHeight="50" d:DesignWidth="300">
    <StackPanel>
        <ItemsControl x:Name="RadioButtonList" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="0,5,0,5"
                      BorderBrush="LightGray" BorderThickness="1" Background="White" Padding="10"
                      ItemsSource="{Binding RadioButtons, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <local:CustomRadioButton PlaceholderText="{Binding PlaceholderText}" 
                                             RbOutput="{Binding RbOutput}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            <ItemsControl.Resources>
                <Style TargetType="{x:Type Border}">
                    <Setter Property="CornerRadius" Value="10"/>
                </Style>
            </ItemsControl.Resources>
        </ItemsControl>
    </StackPanel>
</UserControl>
